# core package: infrastructure shared by every bot in this repo —
# config loading, the database connection helper, and the webhook
# security/parsing pattern. Bot-specific code lives under bots/<name>/.
