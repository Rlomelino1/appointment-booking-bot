# wsgi.py: production entry point.
# Exposes the Flask app that receives Telegram webhook updates; served by
# gunicorn in production (e.g. `gunicorn wsgi:app`).
# Intentionally empty for now — no bot logic yet.
