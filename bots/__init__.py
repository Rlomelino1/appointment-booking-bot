# bots package: one subpackage per bot. Each bot owns its Telegram instance,
# handlers, dialogue logic, and data-access layer; shared infrastructure
# (config, db, webhook security) comes from core/.
